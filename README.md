# AWSDevOpsFoundation

This project demonstrates basic Infrastructure as Code using AWS CloudFormation.

It deploys:
- One EC2 instance
- Installs nginx automatically using UserData
- Outputs the public URL of the server

Purpose:
Learn how to automate infrastructure provisioning instead of creating resources manually.
